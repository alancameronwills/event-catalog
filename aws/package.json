{
  "name": "event-catalog-aws-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local tooling for the shared AWS backend: the one-off migrate.mjs script. Not deployed — the Lambda has its own package.json in src/.",
  "scripts": {
    "migrate": "node migrate.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  }
}
